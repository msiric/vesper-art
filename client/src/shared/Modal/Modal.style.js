import { fade, makeStyles } from '@material-ui/core/styles';

const ModalStyles = makeStyles((muiTheme) => ({
  modal: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
}));

export default ModalStyles;
