import { makeStyles } from '@material-ui/core/styles';

const StripeInputStyles = makeStyles((muiTheme) => ({
  root: {
    width: '100%',
    padding: '6px 0 7px',
    cursor: 'text',
  },
}));

export default StripeInputStyles;
