import { makeStyles } from "@material-ui/core/styles";

const logoItemStyles = makeStyles((muiTheme) => ({
  logo: {
    width: 130,
    cursor: "pointer",
    display: "block",
  },
}));

export default logoItemStyles;
