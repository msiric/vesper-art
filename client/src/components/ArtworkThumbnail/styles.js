import { makeStyles } from "@material-ui/core/styles";

const artworkThumbnailStyles = makeStyles((muiTheme) => ({
  artworkThumbnail: {
    width: "85%",
    maxWidth: 200,
  },
}));

export default artworkThumbnailStyles;
