import { makeStyles } from "@material-ui/core/styles";

const licenseAlertStyles = makeStyles((muiTheme) => ({
  alert: {
    textAlign: "center",
  },
  link: {
    color: "white",
    textAlign: "center",
  },
}));

export default licenseAlertStyles;
