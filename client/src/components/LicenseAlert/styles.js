import { makeStyles } from "@material-ui/core/styles";

const licenseAlertStyles = makeStyles(() => ({
  alert: {
    textAlign: "center",
  },
  link: {
    color: "white",
    textAlign: "center",
  },
}));

export default licenseAlertStyles;
