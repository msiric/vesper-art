import { makeStyles } from "@material-ui/core/styles";

const artworkCreatorStyles = makeStyles(() => ({
  actions: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default artworkCreatorStyles;
