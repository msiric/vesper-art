import { makeStyles } from "@material-ui/core/styles";

const artworkCreatorStyles = makeStyles((muiTheme) => ({
  artworkCreatorActions: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default artworkCreatorStyles;
