import { makeStyles } from "@material-ui/core/styles";

const verifierCardStyles = makeStyles(() => ({
  actions: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default verifierCardStyles;
