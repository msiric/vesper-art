import { makeStyles } from "@material-ui/core/styles";

const artworkModifierStyles = makeStyles((muiTheme) => ({
  actions: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default artworkModifierStyles;
