import { makeStyles } from "@material-ui/core/styles";

const artworkModifierStyles = makeStyles(() => ({
  actions: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default artworkModifierStyles;
