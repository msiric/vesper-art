import { makeStyles } from "@material-ui/core/styles";

const dashboardStatisticsStyles = makeStyles((muiTheme) => ({
  dashboardStatisticsContainer: {
    marginBottom: 8,
    display: "flex",
    justifyContent: "center",
  },
}));

export default dashboardStatisticsStyles;
