import { makeStyles } from "@material-ui/core/styles";

const galleryToolbarStyles = makeStyles(() => ({
  container: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
  },
}));

export default galleryToolbarStyles;
