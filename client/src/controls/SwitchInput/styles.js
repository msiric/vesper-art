import { makeStyles } from "@material-ui/core/styles";

const switchInputStyles = makeStyles(() => ({
  wrapper: {
    display: "flex",
    justifyContent: "space-between",
    margin: 0,
  },
}));

export default switchInputStyles;
